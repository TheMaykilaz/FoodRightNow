apiVersion: v1
kind: Service
metadata:
  name: courier-service
  namespace: delivery-system
  labels:
    app: courier-service
spec:
  type: NodePort
  selector:
    app: courier-service
  ports:
    - port: 8002
      targetPort: 8002
      nodePort: 30002
      protocol: TCP
