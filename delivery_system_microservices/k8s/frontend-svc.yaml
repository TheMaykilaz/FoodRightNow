apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: delivery-system
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30000
      protocol: TCP
