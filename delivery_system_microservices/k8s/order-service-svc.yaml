apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: delivery-system
  labels:
    app: order-service
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - port: 8001
      targetPort: 8001
      nodePort: 30001
      protocol: TCP
