apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: delivery-system
  labels:
    app: reporting-service
spec:
  type: NodePort
  selector:
    app: reporting-service
  ports:
    - port: 8004
      targetPort: 8004
      nodePort: 30004
      protocol: TCP
