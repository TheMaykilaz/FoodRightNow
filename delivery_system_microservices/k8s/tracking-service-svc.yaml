apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: delivery-system
  labels:
    app: tracking-service
spec:
  type: NodePort
  selector:
    app: tracking-service
  ports:
    - port: 8003
      targetPort: 8003
      nodePort: 30003
      protocol: TCP
